apiVersion: v1
kind: Service
metadata:
  name: semantic-cache-api
  namespace: semantic-cache
  labels:
    app: semantic-cache-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: semantic-cache-api
